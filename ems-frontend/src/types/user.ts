export type UserServiceDto = {
    id: number;         // același id din Auth
    firstName: string;
    lastName: string;
    address: string;
    email: string;
};

export type DeviceServiceUserDto = {
    id: number;
};
